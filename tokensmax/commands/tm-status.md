---
description: tokensmax — fleet briefing (accounts, models, what each is for, per-call potential)
allowed-tools: Bash(tokensmax status)
---
!`tokensmax status`

Brief me on the fleet above in prose: which accounts/subscriptions are wired, each engine's models and what it's used for, and the per-request potential. Remind me that live remaining quota isn't queryable — the ⚠ flag in `tokensmax usage` is the real ceiling.
